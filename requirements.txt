# Install torch SEPARATELY first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

transformers>=4.45.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0
accelerate>=1.0.0

# eval harness (stage 4)
scikit-learn>=1.5.0
pandas>=2.2.0

# misc
python-dotenv>=1.0.0
tqdm
